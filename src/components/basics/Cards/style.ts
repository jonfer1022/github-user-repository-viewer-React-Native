import { StyleSheet } from 'react-native';
import { backgroundPrimary } from '../../../utils/styles/colors';

export const style = StyleSheet.create({
  cardContainer: {
    backgroundColor: backgroundPrimary,
    borderRadius: 10,
    padding: 5,
    marginVertical: 5,
  },
});
